export enum MyFormStatus {
    Success = "Success",    // Form successfully submitted.
    New = "New",            // Form is in new state.
    Failed = "Failed",      // Form failed to submit.
    Loading = "Loading",    // Indicates that the form is processing data.  
}